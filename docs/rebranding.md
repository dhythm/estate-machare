# Estate Machare — リブランディング設計

Agri Machare（農機具の売買・レンタル・運搬マッチング）を、同じ共通基盤のうえで
不動産（売買・賃貸）版へ作り替えた記録。machare は MAtCH + shARE、所有する人と
必要とする人をつなぐ基盤の名前で、扱う対象だけが入れ替わっている。

## ブランドの考え方

**住まいと、次の可能性を。**

物件を所有する人、必要とする人、仲介する人をつなぐ。購入・賃貸・買取オプションを
比較し、取引のやり取りから条件の登録までを同じ場所で進められるプラットフォーム。

トップのメッセージは「この場所から、次の暮らしを。」。最初に目的を選んで検索でき、
説明を読み終える前に自分の用事を始められる。

## 視覚体系

| 要素       | 定義                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------- |
| ブランド名 | Estate Machare                                                                                                      |
| マーク     | 屋根と扉を重ねた建築的なSVGマーク。共通ロゴとファビコンに使用                                                       |
| 主色       | インクブルー `#203A43`                                                                                              |
| 背景       | アイボリー `#F7F5F0`、カードは白                                                                                    |
| 強調       | サンド `#E1C5AB`。主に賃貸・次の選択肢に使用                                                                        |
| 本文       | `#22343B`、補足・ラベル `#687579`                                                                                   |
| 字体       | 本文 Noto Sans JP、見出し Zen Kaku Gothic New                                                                       |
| 配置       | 公開ページは写真と比較情報、取引・運営は要対応と実データを優先                                                      |
| 操作       | 主要ボタン40–48px、フォーム48px、入力エラーへのフォーカス移動、フォーカス表示、本文スキップ、動きの低減設定への対応 |

検証した配色を一貫して提示するため、公開ページはライトテーマを使用する。

## ドメインモデルの対応

| 農機具版                       | 不動産版                                     | 備考                                    |
| ------------------------------ | -------------------------------------------- | --------------------------------------- |
| `maker` メーカー               | `zoning` 用途地域                            | 住居系・商業系・工業系の8区分           |
| `condition` 状態               | `layout` 間取り                              | 1R〜4LDK以上。土地は持たない            |
| `hours` 稼働時間               | `floorArea` 専有面積（㎡）                   | 土地は敷地面積として使用                |
| `year` 年式                    | `builtYear` 築年                             | 土地は持たない                          |
| —                              | `nearestStation` / `walkMinutes`             | 最寄駅と駅徒歩分                        |
| —                              | `leaseType` 借家種別                         | 普通借家 / 定期借家。賃貸に対応する出品 |
| カテゴリ（トラクター ほか）    | マンション / 戸建 / 土地 / 事業用            |                                         |
| `seller.kind`（個人農家 ほか） | 個人 / 宅建業者 / 管理会社 / 法人            |                                         |
| `rentPerDay` 日額              | `rentPerMonth` 月額賃料                      |                                         |
| —                              | `depositMonths` / `keyMoneyMonths`           | 敷金・礼金は**月数**で保持し金額を導出  |
| `rentToOwn` レンタル購入       | `purchaseOption` 買取オプション              | 支払済み賃料を購入価格に充当            |
| `Rental` / `rentals`           | `Lease` / `leases`（`tenantUserId`）         | 日数 → 契約月数、初期費用を申込時に確定 |
| `TransportJob` 運搬案件        | `PropertyRequest` 物件リクエスト             | 探索方向を逆にした掲示板                |
| `CarrierProfile` 運搬者        | `AgentProfile` 担当者（`handledCategories`） | 対応地域 + 取扱カテゴリでマッチング     |
| 距離帯の運搬料金               | 初期費用の目安（`lib/lease.ts`）             | 前家賃 + 敷金 + 礼金 + 仲介手数料       |

敷金・礼金を月数で持つのは、日本の賃貸表記に一致し、月額賃料から金額を導出できて
二重管理が起きないため。運搬は「所有する人 / 必要とする人 / **仲介する人**」の3者
構成を保ったまま物件リクエストへ転用し、掲示板・提案・事業者プロフィール・地域
マッチング・状態機械・運営タブをすべて意味のある形で残した。

## 情報設計と機能対応

| 領域               | 主な入口・構成                                                                                      | 保持した機能                                                                                                  |
| ------------------ | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| トップ             | 買う / 借りる / 住んでから買う 検索 → 出品一覧 → 利用の流れ・購入充当の案内 → リクエスト → 各種登録 | カテゴリ・取引条件絞込、全一覧への条件引継ぎ、出品・リクエスト導線                                            |
| 物件一覧           | 検索と取引条件を一つのパネルに集約                                                                  | キーワード、カテゴリ、取引条件、間取り、URL同期、ページ送り、読込/空/エラー/再試行                            |
| 物件カード         | 写真、カテゴリ/用途地域、間取り・面積・築年、最寄駅と徒歩分、地域、価格と月額賃料、評価             | 売買のみ・賃貸のみ・両対応の出品表示、買取オプションの可否                                                    |
| 物件詳細           | 写真・仕様と取引方法パネル、出品者・レビュー                                                        | 写真切替、所有者編集、購入問い合わせ、期間申込、購入充当シミュレーター、初期費用の内訳、関連出品              |
| 出品               | 基本情報 → 取引方法/価格 → 写真 → 出品者                                                            | 全入力項目、写真追加/削除・縮小、条件分岐（土地は間取り・築年なし／賃貸は敷金・礼金・借家種別）、新規/編集    |
| 物件リクエスト     | 登録と提案を別入口に、リクエストはエリア・条件・予算・入居希望日を比較                              | 登録/編集、物件情報の引継ぎ、ログイン復帰、提案、候補担当者、プロフィール、取扱カテゴリ/地域、初期費用の目安  |
| 取引ワークスペース | 要対応サマリー → 未読/新着 → 出品 → 賃貸借契約 → リクエスト → 送信履歴                              | 出品編集/取下げ/再掲載、受信/送信、未読、入居者・貸主の全操作、レビュー、リクエストの成約、地域一致リクエスト |
| メッセージ         | 会話と取引情報を分離、スマホはメッセージを先に表示                                                  | 返信、参加者権限、所有者の状態変更、成約後レビュー、既読                                                      |
| 通知               | 未読件数、未読だけ表示、全履歴                                                                      | 個別/全件既読、関連画面へのリンク、取得エラーと再試行                                                         |
| 運営               | 深いネイビーのサイドバー、スマホは2列のメニュー、審査優先の概要、検索付き一覧                       | 全管理タブ、出品/リクエスト審査・理由、状態絞込、取下げ、停止/解除・自己停止防止、各取引/レビュー/担当者一覧  |

## 実装上の原則

- 認証・権限・API の形・データストアの契約は変えず、既存コンポーネントの情報階層と導線を保つ。
- 出品者、入居者、担当者は同一アカウントで行き来できる。固定した役割別アカウントへ分断しない。
- 検索の条件やログイン前の物件情報を、次の画面で失わない。
- スキーマは既存のマイグレーションを書き換えず、`0015`〜`0018` を追加して移行する。
- 変数名・関数名・DB 列名は英語で、ローマ字を避ける（`walk_minutes`、`key_money_months` など）。
- 未実装のエスクロー決済、メール通知、保証を実装済みとして案内しない。

## 現在のサービス範囲

元のサービスはコンセプト検証版で、実決済は未実装。今回も本番決済や重要事項説明、
電子契約、新規会員登録を追加していない。期間指定の賃貸借契約、出品条件に応じた
購入充当、取引メッセージ、物件リクエストのマッチングを実装済み機能として扱う。

## 画像制作

- ヒーローは `public/estate-hero.webp`、物件写真は `public/properties/*.webp` を使用。自然光と木の質感を中心とした、デモ用の架空建築・物件画像です。実在物件の写真ではありません。
- カード・詳細・問い合わせ・取引・管理画面は、保存済みの旧SVG見本パスを `lib/property-image.ts` で新しい画像へ表示時に変換します。ユーザーが登録した写真や外部URLは変更しません。
- 既存SVGアセットも残し、保存データとの互換性を保ちます。

## main優先の統合

PR #7の統合では、mainの `Listing`、月額賃貸契約、買取オプション、物件リクエスト、担当者、API、DBマイグレーションをそのまま採用しています。旧PRにあった日額互換モデルや独自の `property` JSONB列は採用しません。mainからの変更はブランドの配色・写真・画面構成と画像参照の更新に限定します。

## 検証結果（2026-09-16）

- lint・整形・型チェック・未使用コード検出はすべて成功。
- `pnpm test:coverage`: 432件成功。行カバレッジ94.22%。
- `pnpm test:pglite`: 433件成功（`0015`〜`0018` を含む）。
- `pnpm exec next build --webpack`: 本番ビルド成功。
- スマートフォン幅390pxで横はみ出しがないこと、買取オプション検索の条件引継ぎ、物件詳細の月額賃料・敷金礼金・申込導線をブラウザーで確認。
- API・DB・store・validation・賃貸契約・物件リクエストの実装がmainと一致することを差分監査。
